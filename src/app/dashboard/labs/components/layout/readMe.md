# Folder Overview

Layout showcase components moved to separate routes:

- [`containers/`](../containers/readMe.md) — PageContainer, SectionContainer, ElementContainer
- [`layouts/`](../layouts/readMe.md) — Grids, flex, dividers, spacing

## Legacy Route

`/dashboard/labs/components/layout` redirects to `/dashboard/labs/components/containers`.
