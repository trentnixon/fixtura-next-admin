# Completed Tickets

### TKT-2026-004

Added notification issues list integration: types, `fetchNotificationIssues`, `useNotificationIssues`, `/dashboard/data/notifications/issues` route with URL filters and pagination, health `byStep` drill-down links, and comms handoff copy.

### TKT-2026-003

Implemented job detail page with expandable timeline, `fetchScraperLogByJobId` (list `jobId` query + path fallback), types for metrics/issues/queue depth, table columns Kind/Service/Entries, and documentation updates.
