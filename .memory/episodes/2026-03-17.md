# Episode — 2026-03-17

## Context

Add Grades to Competition scope to the Data page (`/dashboard/data`). New direct Redis queue option: `POST /api/competition/trigger-grades-comps-scrape` enqueues to `scrape:grades-comps`. Full run (empty body) scrapes all competitions' grades from PlayHQ.

## Work Done

- Created `src/types/triggerGradesCompsScrape.ts` (GradesCompsTarget, GradesCompsOptions, request/response types)
- Created `src/lib/services/data-collection/triggerGradesCompsScrape.ts` (API service)
- Created `src/hooks/data-collection/useTriggerGradesCompsScrape.ts` (mutation hook with toast + scraperLogs invalidation)
- Added "Grades to Competition" tab to `ScraperLogsSectionWithScopeSelector.tsx` with trigger button and SCOPE_CONFIG
- Extended `ScraperScope` and `TriggerableScope` types; wired `handleConfirm` to call `triggerGradesCompsScrape({})`
- Added `grades_comps` to `fetchScraperLogs.ts`, `useScraperLogs.ts`, and `ScraperLogsSection.tsx` scope prop type

## Decisions

- None

## Results

- Data page has "Grades to Competition" tab with one-click trigger (full run, empty body)
- Trigger enqueues to `scrape:grades-comps`; logs tab filters by `grades_comps` when CMS scraper logs API supports it

## Next

- [ ] Verify CMS scraper logs API supports `scope=grades_comps` if logs empty for that tab

## Links

- [admin-frontend-trigger-grades-comps-scrape-integration.md](src/app/dashboard/data/.comms/admin-frontend-trigger-grades-comps-scrape-integration.md)
- [ScraperLogsSectionWithScopeSelector.tsx](src/app/dashboard/data/components/ScraperLogsSectionWithScopeSelector.tsx)
