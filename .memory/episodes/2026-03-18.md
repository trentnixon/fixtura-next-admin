# Episode — 2026-03-18

## Context

Add a new "Grades Lookup Teams" tab to the Data page (`/dashboard/data`) for the `scrape:grades-lookup-teams` queue, which scrapes PlayHQ ladder pages for teams per grade.

## Work Done

- Added `grades_lookup_teams` to SCOPES in `fetchScraperLogs.ts`, `useScraperLogs.ts`, and `ScraperLogsSection.tsx` scope prop type
- Added "Grades Lookup Teams" tab to `ScraperLogsSectionWithScopeSelector.tsx` with trigger button and SCOPE_CONFIG
- Imported `triggerGradesLookupTeamsScrape` and wired `handleConfirm` for the new scope
- Extended `ScraperScope` and `TriggerableScope` types to include `grades_lookup_teams`

## Decisions

- None

## Results

- Data page now has six tabs: All Scopes, Clients List, Association to Competition, Club to Competition, Grades to Competition, Grades Lookup Teams
- Trigger button enqueues jobs to `scrape:grades-lookup-teams`; logs show when CMS scraper logs API supports `scope=grades_lookup_teams`

## Next

- [ ] Verify CMS scraper logs API supports `scope=grades_lookup_teams` if logs empty

## Links

- [ScraperLogsSectionWithScopeSelector.tsx](src/app/dashboard/data/components/ScraperLogsSectionWithScopeSelector.tsx)
- [admin-frontend-trigger-grades-lookup-teams-integration.md](src/app/dashboard/competitions/[competitionID]/.comms/admin-frontend-trigger-grades-lookup-teams-integration.md)

---

## Context (Scrape Teams Button)

Implement "Scrape Teams" button on competition detail page per integration guide. Triggers `POST /api/competition/trigger-grades-lookup-teams-single-scrape` with `{ competitionId }`; CMS enqueues to `scrape:grades-lookup-teams-single`.

## Work Done (Scrape Teams)

- Created `src/types/triggerGradesLookupTeamsSingleScrape.ts` (request/response types)
- Created `src/lib/services/data-collection/triggerGradesLookupTeamsSingleScrape.ts` (API service)
- Created `src/hooks/competitions/useTriggerGradesLookupTeamsSingleScrape.ts` (mutation hook with toast + cache invalidation)
- Added `ScrapeTeamsButton` to `SnapshotSection.tsx` with confirmation dialog (Users icon, next to Scrape Grades)
- Updated `.docs/readMe.md`, `DevelopmentRoadMap.md`, `lib/services/readMe.md`, `hooks/readMe.md`

## Results (Scrape Teams)

- Competition detail page now has "Scrape Teams" button alongside "Scrape Grades"
- Click opens confirmation dialog; confirm triggers POST; success toast shows jobId/queueName; competition admin detail query invalidates

## Links (Scrape Teams)

- [admin-frontend-trigger-grades-lookup-teams-single-integration.md](src/app/dashboard/competitions/[competitionID]/.comms/admin-frontend-trigger-grades-lookup-teams-single-integration.md)
- [SnapshotSection.tsx](src/app/dashboard/competitions/[competitionID]/components/CompetitionAdminDetail/sections/SnapshotSection.tsx)

---

## Context (Lookup Grade Teams — Competitions List)

Move "Lookup Grade Teams" trigger button from single competition page to competitions list page (`/dashboard/competitions/`). Single-competition button to be added later.

## Work Done (Lookup Grade Teams — Competitions List)

- Created `components/LookupGradeTeamsButton.tsx` in competitions folder (client component with confirmation dialog)
- Added `LookupGradeTeamsButton` as children of `CreatePageTitle` on competitions page (`page.tsx`)
- Removed `LookupGradeTeamsButton` and `useTriggerGradesLookupTeamsScrape` import from `SnapshotSection.tsx`
- Updated `competitions/readMe.md`, `[competitionID]/.docs/readMe.md`, `DevelopmentRoadMap.md`

## Results (Lookup Grade Teams — Competitions List)

- "Lookup Grade Teams" button now appears in header of `/dashboard/competitions/` (full run, empty body)
- Single competition page retains only "Scrape Grades" button; single-comp Lookup Grade Teams to be added later

## Links (Lookup Grade Teams — Competitions List)

- [LookupGradeTeamsButton.tsx](src/app/dashboard/competitions/components/LookupGradeTeamsButton.tsx)
- [admin-frontend-trigger-grades-lookup-teams-integration.md](src/app/dashboard/competitions/[competitionID]/.comms/admin-frontend-trigger-grades-lookup-teams-integration.md)

---

## Context (Scrape Grades + Doc Updates)

Trigger Grades Comps Single Scrape: "Scrape Grades" button on competition detail page. Triggers `POST /api/competition/trigger-grades-comps-single-scrape` with `{ competitionId }`; CMS enqueues to `scrape:grades-comps-single`. Documentation refresh for competition drilldown and related readmes.

## Work Done (Scrape Grades + Docs)

- Implemented Scrape Grades: types, service, hook, `ScrapeGradesButton` in SnapshotSection with confirmation dialog
- Updated `[competitionID]/.docs/DevelopmentRoadMap.md`, `readMe.md`; parent `competitions/readMe.md`; `lib/services/readMe.md`; `hooks/readMe.md`

## Results

- Competition detail has "Scrape Grades" and "Scrape Teams" buttons; docs reflect current structure and dependencies

## Links

- [admin-frontend-trigger-grades-comps-single-integration.md](src/app/dashboard/competitions/[competitionID]/.comms/admin-frontend-trigger-grades-comps-single-integration.md)
