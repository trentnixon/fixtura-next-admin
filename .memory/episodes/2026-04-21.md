# Episode — 2026-04-21

## Context

Shipped admin frontend integration to queue fixture discovery for a single grade from the grade detail route, per local handoff and implementation docs.

## Work Done

- Added `TriggerFixtureDiscoveryGrade` types (`id` request; success payload with string `jobId`, `runId`, `queueName`, `gradeId`).
- Added server action `triggerFixtureDiscoveryGrade` — `POST /grade/trigger-fixture-discovery` via shared Axios client with error normalization aligned to other trigger services.
- Added `useTriggerFixtureDiscoveryGrade` (toast + invalidate `["gradeInRender", id]`).
- Added `TriggerFixtureDiscoveryButton` (Discover Fixtures, confirmation dialog, accent/RefreshCw pattern).
- Wired button into `src/app/dashboard/grades/[gradeID]/page.tsx` action row before status badges.

## Decisions

- (none recorded — standard queue-trigger pattern matching existing club scrape flow.)

## Results

Grade detail page can queue `fixture_discovery` jobs asynchronously; backend remains source of truth for prerequisites (URL, competition, association sport). `tsc` run surfaced only pre-existing errors in `fetchScraperLogByJobId.ts`, not in new files.

## Next

- [ ] Manual QA: happy path and 400 (e.g. missing grade URL) on a real CMS.
- [ ] Continue association/global work as per prior state items where still open.

## Links

- `src/types/triggerFixtureDiscoveryGrade.ts`
- `src/lib/services/data-collection/triggerFixtureDiscoveryGrade.ts`
- `src/hooks/grades/useTriggerFixtureDiscoveryGrade.ts`
- `src/app/dashboard/grades/[gradeID]/components/TriggerFixtureDiscoveryButton.tsx`
- `src/app/dashboard/grades/[gradeID]/page.tsx`
